import { StyleSheet } from "react-native";

export const HomeStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    map: {
        flex: 1,
    },
    city: {
        textAlign: "center",
        padding: 10,
        backgroundColor: "white",
    },
});
