import { StyleSheet } from "react-native";

export const editAccountStyle = StyleSheet.create({
    container: {
        padding: 16,
        flex: 1,
        alignItems: "center",
    },
    input: {
        height: 40,
        borderColor: "gray",
        borderWidth: 1,
        marginBottom: 16,
    },
    add: { color: "blue" },
});
