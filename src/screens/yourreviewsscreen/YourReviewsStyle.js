import { StyleSheet } from "react-native";

export const yourReviewsStyle = StyleSheet.create({
    title: {
        fontSize: 20,
        fontWeight: "bold",
        textAlign: "center",
        marginBottom: 10,
    },
    container: {
        padding: 16,
    },
});
