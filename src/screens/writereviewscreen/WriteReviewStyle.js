import { StyleSheet } from "react-native";

export const writeReviewStyle = StyleSheet.create({
    textInput: {
        borderWidth: 1,
        borderColor: "gray",
        marginTop: 10,
        padding: 10,
    },
    container: {
        flex: 1,
    },
});
