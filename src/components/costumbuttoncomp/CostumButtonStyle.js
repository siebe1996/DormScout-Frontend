import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        marginBottom: 20,
        padding: 10,
        backgroundColor: "green",
        alignItems: "center",
    },
    buttonText: {
        color: "white",
    },
});
