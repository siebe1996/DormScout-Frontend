import { StyleSheet } from "react-native";

export const costumTextInputStyle = StyleSheet.create({
    input: {
        height: 40,
        borderColor: "gray",
        borderWidth: 1,
        padding: 8,
    },
});
